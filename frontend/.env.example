# Mnema Frontend Environment Configuration Example
# Copy this file to a new file named '.env' in this directory.

# The base URL pointing to the deployed Python FastAPI backend server.
# Fallback during local development is http://localhost:8000
VITE_BACKEND_URL=http://localhost:8000
